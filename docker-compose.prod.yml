services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    command: ["mysqld", "--performance-schema=OFF"]
    mem_limit: 512m
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u${DB_USERNAME}", "-p${DB_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - csp-logger

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    mem_limit: 64m
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - csp-logger

  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    mem_limit: 256m
    mem_swappiness: 0
    environment:
      # PHP-FPM defaults to pm.max_children=20 and a fixed 128MB OPcache
      # segment, which together can exceed this container's memory limit
      # under a burst and OOM-kill php-fpm workers in a loop. Capped to fit
      # comfortably within mem_limit for this app's real traffic volume.
      PHP_FPM_PM_MAX_CHILDREN: 5
      PHP_OPCACHE_MEMORY_CONSUMPTION: 32
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - app_storage:/var/www/html/storage
    networks:
      - csp-logger

  horizon:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["php", "/var/www/html/artisan", "horizon"]
    stop_signal: SIGTERM
    mem_limit: 256m
    healthcheck:
      test: ["CMD", "healthcheck-horizon"]
      start_period: 10s
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - app_storage:/var/www/html/storage
    networks:
      - csp-logger

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["php", "/var/www/html/artisan", "schedule:work"]
    stop_signal: SIGTERM
    mem_limit: 128m
    healthcheck:
      test: ["CMD", "healthcheck-schedule"]
      start_period: 10s
    env_file:
      - .env
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - app_storage:/var/www/html/storage
    networks:
      - csp-logger

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    mem_limit: 64m
    ports:
      - "80:80"
      - "443:443"
    env_file:
      - .env
    volumes:
      - ./docker/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app
    networks:
      - csp-logger

networks:
  csp-logger:

volumes:
  mysql_data:
  redis_data:
  app_storage:
  caddy_data:
  caddy_config:
